node_modules/
.next/
out/
.env
.env.*
# Keep every *.example template — a per-file allowlist silently dropped
# .env.unraid.example when it was added.
!.env*.example
*.log
.DS_Store
next-env.d.ts
tsconfig.tsbuildinfo
